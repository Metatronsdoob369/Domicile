{
  "name": "@domicile/interface",
  "version": "0.1.0",
  "description": "Interface layer (HTTP + MCP) for Domicile",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "mcp:dev": "ts-node src/mcp-server.ts",
    "mcp:build": "npm run build",
    "mcp:start": "node dist/mcp-server.js"
  }
}
