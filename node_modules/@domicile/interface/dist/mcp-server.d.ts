import { type FastifyInstance } from "fastify";
export interface ServerConfig {
    bearerToken?: string;
}
export declare function createMcpServer(config?: ServerConfig): FastifyInstance;
export interface StartOptions extends ServerConfig {
    port?: number;
    host?: string;
}
export declare function startMcpServer(options?: StartOptions): Promise<FastifyInstance>;
//# sourceMappingURL=mcp-server.d.ts.map