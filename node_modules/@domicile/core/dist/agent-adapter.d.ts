import { OrchestrationAgent } from './agent-sdk';
import { Firestore } from 'firebase/firestore';
/**
 * Adapter for EnhancedQuantumSocialAgent -> OrchestrationAgent
 * Non-invasive wrapper: converts the agent's Result into { plan, confidence }.
 */
export declare class QuantumSocialAdapter implements OrchestrationAgent {
    agentId: string;
    domains: string[];
    version: string;
    private agent;
    constructor(db?: Firestore | null, userId?: string);
    generatePlan(prompt: string, context?: any): Promise<{
        plan: any;
        confidence: number;
    }>;
    validate(plan: any): Promise<{
        valid: boolean;
        issues: string[];
    }>;
}
//# sourceMappingURL=agent-adapter.d.ts.map