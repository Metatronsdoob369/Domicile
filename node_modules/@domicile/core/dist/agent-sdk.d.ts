/**
 * Orchestration SDK: minimal interface every agent should support.
 * Agents may be wrapped by adapters to conform.
 */
export type Artifact = any;
export interface OrchestrationAgent {
    agentId: string;
    domains: string[];
    version: string;
    generatePlan(prompt: string, context?: any): Promise<{
        plan: Artifact;
        confidence: number;
    }>;
    proposeSubOrchestration?(plan: Artifact): Promise<{
        subTasks: any[];
        rationale?: string;
    }>;
    validate?(plan: Artifact): Promise<{
        valid: boolean;
        issues?: string[];
    }>;
}
//# sourceMappingURL=agent-sdk.d.ts.map