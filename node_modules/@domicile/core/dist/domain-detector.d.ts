export type DomainResult = {
    domain: string;
    confidence: number;
    explanation?: string;
};
/**
 * Minimal DomainDetector: keyword-based classifier with optional LLM hook.
 * Independent — never uses agent code.
 */
export declare class DomainDetector {
    private llmClient;
    private keywordMap;
    constructor(llmClient?: {
        classify?: (text: string) => Promise<DomainResult>;
    } | null);
    classify(text: string): DomainResult;
    classifyEnsemble(text: string): Promise<DomainResult>;
}
//# sourceMappingURL=domain-detector.d.ts.map