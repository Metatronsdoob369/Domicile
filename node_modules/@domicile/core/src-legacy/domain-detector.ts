// Legacy shim — routes consumers to the maintained DomainDetector implementation
export { DomainDetector } from '../src/domain-detector';
export type { DomainResult } from '../src/domain-detector';
